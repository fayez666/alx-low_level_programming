#include "main.h"
/**
 * _abs -> this func computes the absolute value of an integer.
 * @n: this num to computet the abs val
 * Return: the abs val
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
